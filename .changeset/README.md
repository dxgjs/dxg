# Changesets

All version bumps and changelogs for this project are managed by [changesets](https://github.com/changesets/changesets).

To add a changeset, run:

```bash
pnpm changeset
```

This will prompt you to select the packages and version bump type, and then generate a changeset file in this directory.